# Count-Characters
This Python program accepts a string from the user and counts the total number of characters using the len() function. The result is displayed as output to show the length of the entered text.
